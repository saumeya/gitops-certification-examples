apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: teamB

bases:
- ../base

patches:
- target:
    kind: Namespace
    name: default-dev
  patch: |-
    - op: replace
      path: /metadata/name
      value: teamB